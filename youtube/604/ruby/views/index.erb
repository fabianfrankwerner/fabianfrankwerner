<!DOCTYPE html>
<html lang="en">
<head>
    <title>GitHub Analyzer</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="container">
        <h1>GitHub Analyzer</h1>
        <p>Enter a public repository to analyze.</p>
        
        <% if @error %>
            <div class="error"><%= @error %></div>
        <% end %>

        <form action="/analyze" method="POST">
            <input type="text" name="user" placeholder="Username (e.g. rails)" required>
            <input type="text" name="repo" placeholder="Repository (e.g. rails)" required>
            <button type="submit">Analyze with Magic</button>
        </form>
    </div>
</body>
</html>