{
  "name": "inventory-application",
  "version": "1.0.0",
  "description": "Your Inventory app should have categories and items, so when the user goes to the home-page they can choose a category to view, and then get a list of every item in that category. You should include all of the CRUD methods for both items and categories, so anybody that’s visiting the site can Create, Read, Update or Delete any Item or Category.",
  "main": "index.js",
  "scripts": {
    "db:check": "dotenv -e .env -- node scripts/db-check.js",
    "db:schema": "node scripts/create-tables.js",
    "db:seed": "node scripts/seed-data.js",
    "db:reset": "dotenv -e .env -- psql \"$DATABASE_URL\" -v ON_ERROR_STOP=1 -c \"DROP TABLE IF EXISTS items, categories CASCADE;\" && npm run db:schema && npm run db:seed"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "dotenv": "^17.2.1",
    "ejs": "^3.1.10",
    "express": "^5.1.0",
    "express-ejs-layouts": "^2.5.1",
    "express-validator": "^7.2.1",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "dotenv-cli": "^10.0.0"
  }
}
